name: PR Verify

on:
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    name: PR Verify
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: brynnaleslie/reusableworkflows/.github/workflows/reusable-build.yml@main

        with:
          dotnet-version: 9.0.x
